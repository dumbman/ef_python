language: python
python:
  - "3.6"
  # PyPy versions
  - "pypy3.5"
# command to install dependencies
install:
  - pip3 install -r h5py matplotlib sympy pytest scipy jupyter
# command to run tests
#script: pytest
