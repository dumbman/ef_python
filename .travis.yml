language: python
python:
  - "3.6"
  # PyPy versions
  - "pypy3.5"
# command to install dependencies
install:
  - cd dumbman/ef_python
  - pip3 install -r requirements.txt
  - pip install .
# command to run tests
#script: pytest
