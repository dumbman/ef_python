dist: xenial
sudo: false

language: python
python:
  - "3.6"
  # PyPy versions
  - "pypy3.5"
# command to install dependencies
install:
  - ls
  - pwd
  - pip3 install -r ./requirements.txt
# command to run tests
#script: pytest
